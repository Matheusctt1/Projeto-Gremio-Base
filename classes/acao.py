# Definição da classe Jogo

class acao:
    def __init__(self, advData, atleta, acao, X, Y):
                self.advData = advData
                self.atleta = atleta
                self.acao = acao
                self.X = X
                self.Y = Y
                
                