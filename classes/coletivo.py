class coletivo:
    def __init__(self, adversarioData, nivelJogo, dataJogo, campeonato, categoria, referencia, tempoTotalJogo, gol, finaGol, finaFora, finaBloq, 
                assistFina, assistGol, chanceClara, interceptacao, bloqFina, desarmeCerto, desarmeErrado, falta, dribleCerto, perdaBola, faltaSofrida,
                passeVert, passeVertErrado, passeCerto, passeErra, passeLongo, passeLongoErra, cruzamento, cruzamentoErra, vitoriaAereo, derrotaAereo, 
                MCD, MCO, posseGeral, posseOfensiva,posseMeio, posseDefensiva, finalizacaoOrg, finalizacaoTR, finalizacaoBP
                ):
                self.adversarioData = adversarioData
                self.nivelJogo = nivelJogo
                self.dataJogo = dataJogo
                self.campeonato = campeonato
                self.categoria = categoria
                self.referencia = referencia
                self.tempoTotalJogo = tempoTotalJogo
                self.gol = gol
                self.finaGol = finaGol
                self.finaFora = finaFora
                self.finaBloq = finaBloq
                self.assistFina = assistFina
                self.assistGol = assistGol
                self.chanceClara = chanceClara
                self.interceptacao = interceptacao
                self.bloqFina = bloqFina
                self.desarmeCerto = desarmeCerto
                self.desarmeErrado = desarmeErrado
                self.falta = falta
                self.dribleCerto = dribleCerto
                self.perdaBola = perdaBola
                self.faltaSofrida = faltaSofrida
                self.passeVert = passeVert
                self.passeVertErrado = passeVertErrado
                self.passeCerto = passeCerto
                self.passeErra = passeErra
                self.passeLongo = passeLongo
                self.passeLongoErra = passeLongoErra
                self.cruzamento = cruzamento
                self.cruzamentoErra = cruzamentoErra
                self.vitoriaAereo = vitoriaAereo
                self.derrotaAereo = derrotaAereo
                self.MCD = MCD
                self.MCO = MCO
                self.posseGeral = posseGeral
                self.posseOfensiva = posseOfensiva
                self.posseMeio = posseMeio
                self.posseDefensiva = posseDefensiva
                self.finalizacaoOrg = finalizacaoOrg
                self.finalizacaoTR = finalizacaoTR
                self.finalizacaoBP = finalizacaoBP